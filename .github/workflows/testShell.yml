name: Run Shell Script

on: workflow_dispatch

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run shell script
        run: bash scripts/testShell.sh

      - name: Run cat command
        run: cat abc.txt
